import 'package:flutter/material.dart';

class SignalEssentialsPage extends StatelessWidget {
  const SignalEssentialsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Signal & Essential Range'),
      ),
      body: const Center(
        child: Text('Signal & Essential Range Page'),
      ),
    );
  }
}
